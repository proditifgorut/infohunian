export interface Price {
  category: string;
  icon: string;
  daily: number;
  weekly: number;
  monthly: number;
  yearly: number;
}

export interface Testimonial {
  quote: string;
  name: string;
  role: string;
  avatar: string;
}
